// Header.js
import React from 'react';

const Header = ({ title }) => {
  return (
    <header>
      <h1>{title}</h1>
      {/* Additional header content can go here */}
    </header>
  );
};

export default Header;
